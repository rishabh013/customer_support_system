#### customer_support_system (RAG project)
    -- environment created through the pip

#### 2) inside the project create the vertual venv
    -- python3 -m venv .venv

#### 3) Activate the venv
    -- source .venv/bin/activate

#### 4)
    -- pip install -r requirements.txt


## Project building steps

1) create requirements.txt and create setup.py file
    -- pip install -r requirements.txt 
    ''' It will run the setup.py file because i have added -e. in the requirement file'''
    ''' After install we can check the summary through pip show e-commerce-bot'''
